language: node_js
node_js:
  - "12.12.0"

branches:
  only:
    - master

before_script:
  - cd frontend
  - npm install -g firebase-tools
  - npm install

script:
  - npm build

after_success:
  - firebase deploy --token $FIREBASE_TOKEN --non-interactive
